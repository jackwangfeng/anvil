# M0 端到端骨架 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 `int main(){ return 42; }` 走完整七阶段管道编译成 AArch64 可执行文件，运行后退出码为 42。

**Architecture:** 单向管道 `源码 → lexer → parser(AST) → IR lower → codegen(AArch64 .s) → clang 汇编链接 → 可执行文件`。M0 跳过预处理器与语义分析（留作后续里程碑），只支持最小子集：`int main(){ return <整型常量>; }`。每个阶段是一个独立模块，接口为纯函数，便于单测。

**Tech Stack:** Rust（无第三方编译相关 crate）、Cargo、系统 `clang`（汇编+链接）、目标平台 macOS / AArch64。

---

## 文件结构

M0 阶段每个阶段先用单文件（都远小于 500 行），后续里程碑变大时再拆成同名目录模块。

- `Cargo.toml` —— crate 定义（lib + bin 同名 `bianyi`）
- `src/span.rs` —— 源码位置 `Span{line,col}`，报错基础
- `src/error.rs` —— 统一报错类型 `CompileError`，`Display` 输出 `line:col: error: msg`
- `src/token.rs` —— `Token` / `TokenKind`
- `src/lexer.rs` —— `lex(&str) -> Result<Vec<Token>, CompileError>`
- `src/ast.rs` —— AST 定义（`Program/FuncDef/Stmt/Expr`）
- `src/parser.rs` —— `parse(&[Token]) -> Result<ast::Program, CompileError>`
- `src/ir.rs` —— IR 定义 + `lower(&ast::Program) -> ir::Program`
- `src/codegen.rs` —— `generate(&ir::Program) -> String`（AArch64 汇编文本）
- `src/lib.rs` —— 模块声明 + `compile_to_asm(&str) -> Result<String, CompileError>` 串起管道
- `src/main.rs` —— CLI：读 .c → `compile_to_asm` → 写 .s → 调 clang
- `tests/integration.rs` —— 端到端：编译并运行，校验退出码

> 测试用例库（spec §6 的 `tests/cases/`）等到 M1 有大量用例时再建运行器；M0 只需一个内联端到端测试证明链路通。

---

### Task 0: 环境与项目骨架

**Files:**
- Create: `Cargo.toml`, `src/lib.rs`, `src/main.rs`

- [ ] **Step 1: 安装 Rust 工具链（本机当前无 rustc/cargo）**

Run:
```bash
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y
source "$HOME/.cargo/env"
rustc --version && cargo --version
```
Expected: 打印出 rustc / cargo 版本号（如 `rustc 1.x.x`）。
> 若用户更习惯自己装，可在对话里用 `! curl ... | sh -s -- -y` 自行执行。

- [ ] **Step 2: 确认 clang 可用（汇编链接依赖它）**

Run: `clang --version`
Expected: 打印 Apple clang 版本。若缺失：`xcode-select --install`。

- [ ] **Step 3: 创建 Cargo 项目骨架**

创建 `Cargo.toml`：
```toml
[package]
name = "bianyi"
version = "0.1.0"
edition = "2021"

[lib]
name = "bianyi"
path = "src/lib.rs"

[[bin]]
name = "bianyi"
path = "src/main.rs"
```

创建占位 `src/lib.rs`：
```rust
// 各阶段模块将在后续 Task 中加入。
```

创建占位 `src/main.rs`：
```rust
fn main() {
    eprintln!("bianyi: 尚未实现");
    std::process::exit(2);
}
```

- [ ] **Step 4: 配置 git 身份并验证构建**

Run:
```bash
git config user.name "bianyi"
git config user.email "eric@luckyluke.org"
cargo build
```
Expected: `cargo build` 成功，无错误。

- [ ] **Step 5: 提交**

```bash
git add Cargo.toml src/lib.rs src/main.rs
git commit -m "chore: scaffold bianyi cargo project (lib + bin)"
```

---

### Task 1: Span 与 Error 基础设施

**Files:**
- Create: `src/span.rs`, `src/error.rs`
- Modify: `src/lib.rs`

- [ ] **Step 1: 写失败测试**

在 `src/error.rs` 末尾（先写测试，模块内容随后补）：
```rust
#[cfg(test)]
mod tests {
    use super::*;
    use crate::span::Span;

    #[test]
    fn error_displays_line_col_message() {
        let e = CompileError::new(Span::new(3, 7), "boom");
        assert_eq!(e.to_string(), "3:7: error: boom");
    }
}
```

同时在 `src/lib.rs` 加上模块声明，否则测试无法编译：
```rust
pub mod error;
pub mod span;
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cargo test error_displays_line_col_message`
Expected: 编译失败（`span`/`error` 内容未定义）。

- [ ] **Step 3: 写最小实现**

`src/span.rs`：
```rust
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub struct Span {
    pub line: u32,
    pub col: u32,
}

impl Span {
    pub fn new(line: u32, col: u32) -> Self {
        Span { line, col }
    }
}
```

`src/error.rs`（测试模块放在文件末尾，下面是主体）：
```rust
use crate::span::Span;
use std::fmt;

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct CompileError {
    pub span: Span,
    pub message: String,
}

impl CompileError {
    pub fn new(span: Span, message: impl Into<String>) -> Self {
        CompileError {
            span,
            message: message.into(),
        }
    }
}

impl fmt::Display for CompileError {
    fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {
        write!(f, "{}:{}: error: {}", self.span.line, self.span.col, self.message)
    }
}

impl std::error::Error for CompileError {}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cargo test error_displays_line_col_message`
Expected: PASS（1 passed）。

- [ ] **Step 5: 提交**

```bash
git add src/span.rs src/error.rs src/lib.rs
git commit -m "feat: add Span and CompileError infrastructure"
```

---

### Task 2: Token 类型与 Lexer

**Files:**
- Create: `src/token.rs`, `src/lexer.rs`
- Modify: `src/lib.rs`

- [ ] **Step 1: 写失败测试**

在 `src/lexer.rs` 末尾：
```rust
#[cfg(test)]
mod tests {
    use super::*;
    use crate::token::TokenKind;

    fn kinds(src: &str) -> Vec<TokenKind> {
        lex(src).unwrap().into_iter().map(|t| t.kind).collect()
    }

    #[test]
    fn lex_return_42() {
        assert_eq!(
            kinds("int main(){ return 42; }"),
            vec![
                TokenKind::KwInt,
                TokenKind::Ident("main".to_string()),
                TokenKind::LParen,
                TokenKind::RParen,
                TokenKind::LBrace,
                TokenKind::KwReturn,
                TokenKind::IntLit(42),
                TokenKind::Semicolon,
                TokenKind::RBrace,
                TokenKind::Eof,
            ]
        );
    }

    #[test]
    fn lex_tracks_line_and_col() {
        let toks = lex("int\n  main").unwrap();
        assert_eq!((toks[1].span.line, toks[1].span.col), (2, 3));
    }

    #[test]
    fn lex_rejects_unknown_char() {
        let err = lex("int @").unwrap_err();
        assert!(err.message.contains('@'));
    }
}
```

在 `src/lib.rs` 增加：
```rust
pub mod lexer;
pub mod token;
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cargo test --lib lexer`
Expected: 编译失败（`token`/`lexer` 内容未定义）。

- [ ] **Step 3: 写最小实现**

`src/token.rs`：
```rust
use crate::span::Span;

#[derive(Debug, Clone, PartialEq, Eq)]
pub enum TokenKind {
    KwInt,
    KwReturn,
    Ident(String),
    IntLit(i64),
    LParen,
    RParen,
    LBrace,
    RBrace,
    Semicolon,
    Eof,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct Token {
    pub kind: TokenKind,
    pub span: Span,
}
```

`src/lexer.rs`（主体，测试模块在末尾）：
```rust
use crate::error::CompileError;
use crate::span::Span;
use crate::token::{Token, TokenKind};

pub fn lex(src: &str) -> Result<Vec<Token>, CompileError> {
    let chars: Vec<char> = src.chars().collect();
    let mut tokens = Vec::new();
    let mut i = 0;
    let mut line = 1u32;
    let mut col = 1u32;

    while i < chars.len() {
        let c = chars[i];
        match c {
            ' ' | '\t' | '\r' => {
                i += 1;
                col += 1;
            }
            '\n' => {
                i += 1;
                line += 1;
                col = 1;
            }
            '(' | ')' | '{' | '}' | ';' => {
                let kind = match c {
                    '(' => TokenKind::LParen,
                    ')' => TokenKind::RParen,
                    '{' => TokenKind::LBrace,
                    '}' => TokenKind::RBrace,
                    ';' => TokenKind::Semicolon,
                    _ => unreachable!(),
                };
                tokens.push(Token { kind, span: Span::new(line, col) });
                i += 1;
                col += 1;
            }
            c if c.is_ascii_digit() => {
                let start_col = col;
                let mut num = String::new();
                while i < chars.len() && chars[i].is_ascii_digit() {
                    num.push(chars[i]);
                    i += 1;
                    col += 1;
                }
                let value: i64 = num.parse().map_err(|_| {
                    CompileError::new(
                        Span::new(line, start_col),
                        format!("invalid integer literal '{}'", num),
                    )
                })?;
                tokens.push(Token {
                    kind: TokenKind::IntLit(value),
                    span: Span::new(line, start_col),
                });
            }
            c if c.is_alphabetic() || c == '_' => {
                let start_col = col;
                let mut ident = String::new();
                while i < chars.len() && (chars[i].is_alphanumeric() || chars[i] == '_') {
                    ident.push(chars[i]);
                    i += 1;
                    col += 1;
                }
                let kind = match ident.as_str() {
                    "int" => TokenKind::KwInt,
                    "return" => TokenKind::KwReturn,
                    _ => TokenKind::Ident(ident),
                };
                tokens.push(Token { kind, span: Span::new(line, start_col) });
            }
            _ => {
                return Err(CompileError::new(
                    Span::new(line, col),
                    format!("unexpected character '{}'", c),
                ));
            }
        }
    }

    tokens.push(Token { kind: TokenKind::Eof, span: Span::new(line, col) });
    Ok(tokens)
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cargo test --lib lexer`
Expected: PASS（3 passed）。

- [ ] **Step 5: 提交**

```bash
git add src/token.rs src/lexer.rs src/lib.rs
git commit -m "feat: add lexer for M0 subset (keywords, ident, int, punctuation)"
```

---

### Task 3: AST 与 Parser

**Files:**
- Create: `src/ast.rs`, `src/parser.rs`
- Modify: `src/lib.rs`

- [ ] **Step 1: 写失败测试**

在 `src/parser.rs` 末尾：
```rust
#[cfg(test)]
mod tests {
    use super::*;
    use crate::ast::{Expr, Stmt};
    use crate::lexer::lex;

    #[test]
    fn parse_return_42() {
        let toks = lex("int main(){ return 42; }").unwrap();
        let prog = parse(&toks).unwrap();
        assert_eq!(prog.functions.len(), 1);
        let f = &prog.functions[0];
        assert_eq!(f.name, "main");
        assert_eq!(f.body, vec![Stmt::Return(Expr::IntLit(42))]);
    }

    #[test]
    fn parse_reports_missing_semicolon() {
        let toks = lex("int main(){ return 42 }").unwrap();
        let err = parse(&toks).unwrap_err();
        assert!(err.message.contains("Semicolon") || err.message.contains(';'));
    }
}
```

在 `src/lib.rs` 增加：
```rust
pub mod ast;
pub mod parser;
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cargo test --lib parser`
Expected: 编译失败（`ast`/`parser` 未定义）。

- [ ] **Step 3: 写最小实现**

`src/ast.rs`：
```rust
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct Program {
    pub functions: Vec<FuncDef>,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct FuncDef {
    pub name: String,
    pub body: Vec<Stmt>,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub enum Stmt {
    Return(Expr),
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub enum Expr {
    IntLit(i64),
}
```

`src/parser.rs`（主体，测试模块在末尾）：
```rust
use crate::ast::{Expr, FuncDef, Program, Stmt};
use crate::error::CompileError;
use crate::token::{Token, TokenKind};

pub fn parse(tokens: &[Token]) -> Result<Program, CompileError> {
    let mut p = Parser { tokens, pos: 0 };
    p.parse_program()
}

struct Parser<'a> {
    tokens: &'a [Token],
    pos: usize,
}

impl<'a> Parser<'a> {
    fn peek_kind(&self) -> &TokenKind {
        &self.tokens[self.pos].kind
    }

    fn expect(&mut self, kind: &TokenKind) -> Result<(), CompileError> {
        let tok = &self.tokens[self.pos];
        if &tok.kind == kind {
            self.pos += 1;
            Ok(())
        } else {
            Err(CompileError::new(
                tok.span,
                format!("expected {:?}, found {:?}", kind, tok.kind),
            ))
        }
    }

    fn expect_ident(&mut self) -> Result<String, CompileError> {
        let tok = &self.tokens[self.pos];
        if let TokenKind::Ident(name) = &tok.kind {
            let name = name.clone();
            self.pos += 1;
            Ok(name)
        } else {
            Err(CompileError::new(
                tok.span,
                format!("expected identifier, found {:?}", tok.kind),
            ))
        }
    }

    fn parse_program(&mut self) -> Result<Program, CompileError> {
        let mut functions = Vec::new();
        while *self.peek_kind() != TokenKind::Eof {
            functions.push(self.parse_func_def()?);
        }
        Ok(Program { functions })
    }

    fn parse_func_def(&mut self) -> Result<FuncDef, CompileError> {
        self.expect(&TokenKind::KwInt)?;
        let name = self.expect_ident()?;
        self.expect(&TokenKind::LParen)?;
        self.expect(&TokenKind::RParen)?;
        self.expect(&TokenKind::LBrace)?;
        let mut body = Vec::new();
        while *self.peek_kind() != TokenKind::RBrace {
            body.push(self.parse_stmt()?);
        }
        self.expect(&TokenKind::RBrace)?;
        Ok(FuncDef { name, body })
    }

    fn parse_stmt(&mut self) -> Result<Stmt, CompileError> {
        self.expect(&TokenKind::KwReturn)?;
        let expr = self.parse_expr()?;
        self.expect(&TokenKind::Semicolon)?;
        Ok(Stmt::Return(expr))
    }

    fn parse_expr(&mut self) -> Result<Expr, CompileError> {
        let tok = &self.tokens[self.pos];
        if let TokenKind::IntLit(v) = tok.kind {
            self.pos += 1;
            Ok(Expr::IntLit(v))
        } else {
            Err(CompileError::new(
                tok.span,
                format!("expected integer literal, found {:?}", tok.kind),
            ))
        }
    }
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cargo test --lib parser`
Expected: PASS（2 passed）。

- [ ] **Step 5: 提交**

```bash
git add src/ast.rs src/parser.rs src/lib.rs
git commit -m "feat: add AST and recursive-descent parser for M0 subset"
```

---

### Task 4: IR 与 lowering

**Files:**
- Create: `src/ir.rs`
- Modify: `src/lib.rs`

- [ ] **Step 1: 写失败测试**

在 `src/ir.rs` 末尾：
```rust
#[cfg(test)]
mod tests {
    use super::*;
    use crate::lexer::lex;
    use crate::parser::parse;

    #[test]
    fn lower_return_42() {
        let ast = parse(&lex("int main(){ return 42; }").unwrap()).unwrap();
        let ir = lower(&ast);
        assert_eq!(ir.functions.len(), 1);
        assert_eq!(ir.functions[0].name, "main");
        assert_eq!(ir.functions[0].body, vec![Instr::Return(Value::Const(42))]);
    }
}
```

在 `src/lib.rs` 增加：
```rust
pub mod ir;
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cargo test --lib ir`
Expected: 编译失败（`ir` 未定义）。

- [ ] **Step 3: 写最小实现**

`src/ir.rs`（主体，测试模块在末尾）：
```rust
use crate::ast::{Expr, FuncDef, Program as AstProgram, Stmt};

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct Program {
    pub functions: Vec<Function>,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct Function {
    pub name: String,
    pub body: Vec<Instr>,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub enum Instr {
    Return(Value),
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub enum Value {
    Const(i64),
}

pub fn lower(ast: &AstProgram) -> Program {
    Program {
        functions: ast.functions.iter().map(lower_func).collect(),
    }
}

fn lower_func(f: &FuncDef) -> Function {
    let mut body = Vec::new();
    for stmt in &f.body {
        match stmt {
            Stmt::Return(expr) => body.push(Instr::Return(lower_expr(expr))),
        }
    }
    Function {
        name: f.name.clone(),
        body,
    }
}

fn lower_expr(expr: &Expr) -> Value {
    match expr {
        Expr::IntLit(v) => Value::Const(*v),
    }
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cargo test --lib ir`
Expected: PASS（1 passed）。

- [ ] **Step 5: 提交**

```bash
git add src/ir.rs src/lib.rs
git commit -m "feat: add IR and AST-to-IR lowering for M0 subset"
```

---

### Task 5: AArch64 代码生成

**Files:**
- Create: `src/codegen.rs`
- Modify: `src/lib.rs`

- [ ] **Step 1: 写失败测试**

在 `src/codegen.rs` 末尾：
```rust
#[cfg(test)]
mod tests {
    use super::*;
    use crate::ir::{Function, Instr, Program, Value};

    #[test]
    fn codegen_return_42() {
        let prog = Program {
            functions: vec![Function {
                name: "main".to_string(),
                body: vec![Instr::Return(Value::Const(42))],
            }],
        };
        let asm = generate(&prog);
        assert!(asm.contains(".globl _main"));
        assert!(asm.contains("_main:"));
        assert!(asm.contains("mov w0, #42"));
        assert!(asm.contains("ret"));
    }
}
```

在 `src/lib.rs` 增加：
```rust
pub mod codegen;
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cargo test --lib codegen`
Expected: 编译失败（`codegen` 未定义）。

- [ ] **Step 3: 写最小实现**

`src/codegen.rs`（主体，测试模块在末尾）：
```rust
use crate::ir::{Function, Instr, Program, Value};
use std::fmt::Write;

pub fn generate(program: &Program) -> String {
    let mut out = String::new();
    out.push_str(".section __TEXT,__text,regular,pure_instructions\n");
    for func in &program.functions {
        gen_func(func, &mut out);
    }
    out
}

fn gen_func(func: &Function, out: &mut String) {
    let _ = writeln!(out, ".globl _{}", func.name);
    out.push_str(".p2align 2\n");
    let _ = writeln!(out, "_{}:", func.name);
    for instr in &func.body {
        match instr {
            Instr::Return(value) => {
                match value {
                    Value::Const(v) => {
                        let _ = writeln!(out, "    mov w0, #{}", v);
                    }
                }
                out.push_str("    ret\n");
            }
        }
    }
}
```

> 注：`mov w0, #imm` 仅适用 16 位内立即数（42 符合）。更大常量与多条 movz/movk 留到 M1。

- [ ] **Step 4: 运行测试确认通过**

Run: `cargo test --lib codegen`
Expected: PASS（1 passed）。

- [ ] **Step 5: 提交**

```bash
git add src/codegen.rs src/lib.rs
git commit -m "feat: add AArch64 codegen for M0 (return constant)"
```

---

### Task 6: 管道串联与 CLI 驱动

**Files:**
- Modify: `src/lib.rs`, `src/main.rs`

- [ ] **Step 1: 写失败测试**

在 `src/lib.rs` 末尾加：
```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn compile_to_asm_end_to_end_text() {
        let asm = compile_to_asm("int main(){ return 42; }").unwrap();
        assert!(asm.contains("_main:"));
        assert!(asm.contains("mov w0, #42"));
    }
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cargo test --lib compile_to_asm_end_to_end_text`
Expected: 编译失败（`compile_to_asm` 未定义）。

- [ ] **Step 3: 写最小实现**

`src/lib.rs` 的最终内容（模块声明 + 管道函数；测试模块保留在末尾）：
```rust
pub mod ast;
pub mod codegen;
pub mod error;
pub mod ir;
pub mod lexer;
pub mod parser;
pub mod span;
pub mod token;

use error::CompileError;

pub fn compile_to_asm(src: &str) -> Result<String, CompileError> {
    let tokens = lexer::lex(src)?;
    let ast = parser::parse(&tokens)?;
    let ir = ir::lower(&ast);
    Ok(codegen::generate(&ir))
}
```

`src/main.rs` 的最终内容：
```rust
use bianyi::compile_to_asm;
use std::path::Path;
use std::process::{exit, Command};

fn main() {
    let args: Vec<String> = std::env::args().collect();
    let (input, output) = match parse_args(&args) {
        Ok(v) => v,
        Err(msg) => {
            eprintln!("{}", msg);
            exit(2);
        }
    };

    let src = match std::fs::read_to_string(&input) {
        Ok(s) => s,
        Err(e) => {
            eprintln!("error: cannot read '{}': {}", input, e);
            exit(1);
        }
    };

    let asm = match compile_to_asm(&src) {
        Ok(a) => a,
        Err(e) => {
            eprintln!("{}:{}", input, e);
            exit(1);
        }
    };

    let asm_path = format!("{}.s", output);
    if let Err(e) = std::fs::write(&asm_path, &asm) {
        eprintln!("error: cannot write '{}': {}", asm_path, e);
        exit(1);
    }

    let status = Command::new("clang")
        .arg(&asm_path)
        .arg("-o")
        .arg(&output)
        .status();

    match status {
        Ok(s) if s.success() => {
            let _ = std::fs::remove_file(&asm_path);
        }
        Ok(s) => {
            eprintln!("error: clang failed: {}", s);
            exit(1);
        }
        Err(e) => {
            eprintln!("error: failed to invoke clang: {}", e);
            exit(1);
        }
    }
}

fn parse_args(args: &[String]) -> Result<(String, String), String> {
    let mut input: Option<String> = None;
    let mut output: Option<String> = None;
    let mut i = 1;
    while i < args.len() {
        match args[i].as_str() {
            "-o" => {
                i += 1;
                if i >= args.len() {
                    return Err("error: -o requires an argument".to_string());
                }
                output = Some(args[i].clone());
            }
            other => {
                if input.is_some() {
                    return Err(format!("error: unexpected argument '{}'", other));
                }
                input = Some(other.to_string());
            }
        }
        i += 1;
    }
    let input = input.ok_or_else(|| "usage: bianyi <input.c> [-o output]".to_string())?;
    let output = output.unwrap_or_else(|| {
        Path::new(&input)
            .file_stem()
            .map(|s| s.to_string_lossy().to_string())
            .unwrap_or_else(|| "a.out".to_string())
    });
    Ok((input, output))
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cargo test --lib compile_to_asm_end_to_end_text && cargo build`
Expected: 测试 PASS，且 `cargo build` 成功生成 `target/debug/bianyi`。

- [ ] **Step 5: 提交**

```bash
git add src/lib.rs src/main.rs
git commit -m "feat: wire compile pipeline and CLI driver (clang assemble+link)"
```

---

### Task 7: 端到端集成测试（编译并运行，校验退出码）

**Files:**
- Create: `tests/integration.rs`

- [ ] **Step 1: 写失败测试**

`tests/integration.rs`：
```rust
use std::process::Command;

/// 用 bianyi 编译 `src`，运行产物，返回其退出码。
fn compile_and_run(src: &str, name: &str) -> i32 {
    let dir = std::env::temp_dir();
    let c_path = dir.join(format!("{}.c", name));
    let exe_path = dir.join(name);
    std::fs::write(&c_path, src).expect("write .c");

    let bin = env!("CARGO_BIN_EXE_bianyi");
    let compile = Command::new(bin)
        .arg(&c_path)
        .arg("-o")
        .arg(&exe_path)
        .status()
        .expect("run bianyi");
    assert!(compile.success(), "bianyi failed to compile {}", name);

    let run = Command::new(&exe_path).status().expect("run compiled exe");
    run.code().expect("program terminated by signal")
}

#[test]
fn m0_return_42() {
    assert_eq!(compile_and_run("int main(){ return 42; }", "m0_return_42"), 42);
}

#[test]
fn m0_return_0() {
    assert_eq!(compile_and_run("int main(){ return 0; }", "m0_return_0"), 0);
}
```

- [ ] **Step 2: 运行测试确认失败 / 或直接验证链路**

Run: `cargo test --test integration`
Expected: 此时实现已就绪（Task 0–6 完成），测试应当能跑；若你严格 TDD，可先在 Task 6 前置此文件观察其因缺二进制而失败。预期最终：2 passed。

- [ ] **Step 3: 实现已在前序任务完成**

无新增实现代码——本任务验证的是 Task 0–6 串起来的真实产物。

- [ ] **Step 4: 运行测试确认通过**

Run: `cargo test`
Expected: 全部 PASS（lib 单测 + integration 2 passed）。

手动复核（可选）：
```bash
echo 'int main(){ return 42; }' > /tmp/t.c
./target/debug/bianyi /tmp/t.c -o /tmp/t && /tmp/t; echo "exit=$?"
```
Expected: `exit=42`。

- [ ] **Step 5: 提交**

```bash
git add tests/integration.rs
git commit -m "test: add end-to-end compile-and-run integration tests for M0"
```

---

## 自查（Self-Review）

**Spec 覆盖：** 本计划实现 spec §2 的管道（lexer/parser/IR/codegen/clang 链接 —— 预处理器与语义分析按 spec §3 在 M0 跳过）、§3 的 M0 目标（`return 42` → 退出码 42）、§5 的项目结构（单文件起步，注明后续拆目录）、§6 的测试方式（编译并运行校验退出码）、§7 的环境前提（Task 0 装 Rust、验证 clang）。覆盖完整。

**占位符扫描：** 无 TBD/TODO/"略"。每个改代码的步骤都给了完整代码与可运行命令。

**类型一致性：** 全程统一 —— `lex() -> Result<Vec<Token>, CompileError>`、`parse(&[Token]) -> Result<ast::Program, _>`、`ir::lower(&ast::Program) -> ir::Program`、`codegen::generate(&ir::Program) -> String`、`compile_to_asm(&str) -> Result<String, CompileError>`。`ast::Program` 与 `ir::Program` 同名但分属不同模块，lower 中以 `Program as AstProgram` 消歧，已显式处理。

**已知边界（M0 之外，符合预期）：** 仅支持 `mov w0, #imm`（16 位内立即数）；仅 `int main`；无变量/运算/控制流——均为后续里程碑内容。
